steps:
  - name: 'gcr.io/cloud-builders/gcloud'  # Use the official gcloud builder image
    entrypoint: '/bin/bash'
    args: 
      - '-c'
      - |
      steps:
 
        ls
        pwd

#        gcloud compute scp --recurse ./ ftwomicroinstance:~/deploymentscripts/ \
 #        --zone=asia-south1-a \
 #        --tunnel-through-iap